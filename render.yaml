services:
  - type: web
    name: webtech-bung-backend
    runtime: java21  # WICHTIG: Das ist jetzt korrekt!
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/*.jar
    envVars:
      - key: PORT
        value: 8080
    autoDeploy: true
